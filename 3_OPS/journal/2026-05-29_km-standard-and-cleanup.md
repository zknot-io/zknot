---
date: YYYY-MM-DD
systems: []                  # e.g. [zknot-site, zknot-api] — which runbooks this touches; [] if none
workstream: []               # hw|fw|sw|ip|biz|ops  (same vocab as Taskwarrior UDA)
status: in-progress          # shipped|decided|in-progress|blocked|reference
tags: []                     # free keywords for grep, e.g. [deploy, cloudflare-worker]
---
# YYYY-MM-DD — <short title>

## TL;DR
<1–2 sentences. The single thing future-you needs. Required.>

## Decisions
<The "do not re-evaluate" verdicts — anything that closes a question or is expensive to
reverse. One bullet each, with the why. Required (write "none" if truly none).>

<!-- Optional sections below — keep only what applies. Delete the rest. -->

## What happened
<Narrative, if the story matters.>

## Gotchas / lessons
<Things that bit you; attribute if someone else's insight helped.>

## Commands
<Reproducible commands worth keeping — the printer-setup entry is the gold standard.>

## Open / next
<Loose ends. Promote durable facts into the relevant systems/<x>.md before closing.>
